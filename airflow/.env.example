# Airflow image extras — kept here so docker-compose can read them.
# Copy to airflow/.env if you need to override the defaults.
_PIP_ADDITIONAL_REQUIREMENTS=dbt-snowflake==1.8.4 snowflake-connector-python==3.12.3 faker==30.8.2
