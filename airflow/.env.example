# Extra Python packages baked into the Airflow image at startup.
# Copy to airflow/.env only if you need to override the default below.
_PIP_ADDITIONAL_REQUIREMENTS=dbt-snowflake==1.8.4 snowflake-connector-python==3.18.0 faker==40.21.0 python-dotenv==1.2.2 pandas==2.2.3
